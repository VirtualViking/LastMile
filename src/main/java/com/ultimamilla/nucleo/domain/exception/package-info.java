/**
 * Excepciones que representan violaciones de reglas de negocio del dominio.
 *
 * <p>Se mapean a respuestas HTTP concretas en {@code infrastructure.in.web}
 * (vía {@code @ControllerAdvice}) a partir de la Fase 3 — el dominio no sabe
 * nada de códigos de estado HTTP.
 */
package com.ultimamilla.nucleo.domain.exception;
