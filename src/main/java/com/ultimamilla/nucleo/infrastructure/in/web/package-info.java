/**
 * Adaptadores de entrada web: controllers REST.
 *
 * <p>Traducen peticiones HTTP en llamadas a los casos de uso ({@code application.port.in})
 * y las respuestas de los casos de uso en respuestas HTTP (DTOs, códigos de estado).
 * No contienen lógica de negocio — solo traducción entre el mundo HTTP y la aplicación.
 */
package com.ultimamilla.nucleo.infrastructure.in.web;
