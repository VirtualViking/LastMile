/**
 * Adaptador de salida de persistencia: implementa los puertos de salida
 * ({@code application.port.out}) usando Spring Data JPA + PostgreSQL.
 *
 * <p>Aquí viven las entidades JPA (distintas de las entidades de dominio), los
 * repositorios Spring Data, y los mappers entre el modelo de persistencia y el
 * modelo de dominio.
 */
package com.ultimamilla.nucleo.infrastructure.out.persistence;
