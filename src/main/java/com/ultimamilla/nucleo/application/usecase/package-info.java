/**
 * Casos de uso: implementan los puertos de entrada ({@code port.in}) y orquestan el dominio.
 *
 * <p>Aquí vive la lógica de aplicación (ej. {@code CrearPedidoUseCaseImpl}), que coordina
 * entidades de {@code domain} y llama a los puertos de salida ({@code port.out}). No
 * contiene reglas de negocio propias — esas viven en el dominio. Este paquete solo orquesta.
 */
package com.ultimamilla.nucleo.application.usecase;
